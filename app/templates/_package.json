{
  "name": "<%= appSlug %>",
  "version": "0.0.0",
  "devDependencies": {
    "assemble": "^0.4.42",
    "browserify": "^9.0.3",
    "browserify-shim": "^3.8.3",
    "grunt": "^0.4.5",
    "grunt-autoprefixer": "^2.2.0",
    "grunt-browser-sync": "^2.0.0",
    "grunt-browserify": "^3.3.0",
    "grunt-cache-bust": "^0.4.5",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-copy": "^0.7.0",
    "grunt-contrib-cssmin": "^0.12.0",
    "grunt-contrib-jshint": "^0.11.0",
    "grunt-contrib-less": "^1.0.0",
    "grunt-contrib-uglify": "^0.7.0",
    "grunt-este-watch": "^0.1.18",
    "jshint-stylish": "^1.0.0",
    "jit-grunt": "^0.9.0",
    "picturefill": "^2.3.0",
    "grunt-jscs": "^1.5.0",<% if (includeBabel) { %>
    "babelify": "^6.0.0",<% } %><% if (includeHandlebars) { %>
    "hbsfy": "^2.2.0",<% } %><% if (includeModernizr) { %>
    "grunt-modernizr": "^0.6.0",<% } %><% if (includeGrunticon) { %>
    "grunt-grunticon": "^2.1.2",
    "grunt-svgmin": "^2.0.0",<% } %><% if (includeRsync) { %>
    "grunt-rsync": "^0.6.2",<% } %>
  },
  "browserify": {
    "transform": [
      <% if (includeJquery || includeHandlebars) { %>"browserify-shim",<% } %>
      <% if (includeBabel) { %>"babelify",<% } %>
      <% if (includeHandlebars) { %>"hbsfy",<% } %>
    ]
  }<% if (includeJquery || includeHandlebars) { %>,
  "browserify-shim": {
    <% if (includeJquery) { %>"jquery": "global:$",<% } %>
    <% if (includeHandlebars) { %>"handlebars": "Handlebars"<% } %>
  }<% } %>
}
